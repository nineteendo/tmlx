/**A pixel can be either white or black (binary).If statement: if color action.This program is ran twice: first on a white and then on a black pixel.The white pixel should be accepted and the black rejected.It looks like the condition is currently incorrect.*/if white exit 1