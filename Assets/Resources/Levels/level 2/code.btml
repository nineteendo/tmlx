/**A pixel can be either white or black (binary).If statement: if color action.Else statement: if color action else action.This program is ran twice: first on a white and then on a black canvas.The white canvas should be accepted and the black rejected.It looks like the condition is currently incorrect.*/if white exit 1