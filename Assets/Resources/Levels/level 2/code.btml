/**A pixel can be either white or black (binary).If statement: if [condition] [action] else [action]This program is ran twice: first on a white and then on a black canvas.The white canvas should be accepted, and the black rejected.It looks like the condition is currently incorrect.*/if White exit 1 else exit 0