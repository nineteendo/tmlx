/**Instead of defining a label to repeat an instruction, you can use the while instruction.While statement: while color action.While else statement: while color action else action.Accept even unsigned numbers: any bits, followed by 0.*/while black right