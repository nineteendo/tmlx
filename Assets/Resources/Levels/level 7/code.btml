/**We will be using the row above the input length as a binary data line.We want to accept positive signed numbers: 0 (white), followed by any bits.A length of 0 is invalid and results in custom return code 2.*/if Black move Up