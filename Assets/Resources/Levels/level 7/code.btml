/**We will be using the row above the input length as a binary input data line.We want to accept positive signed numbers: 0 (white), followed by any bits.A length of 0 is invalid and results in exit status 2 (malformed input).*/if black move north
if 1 exit 1